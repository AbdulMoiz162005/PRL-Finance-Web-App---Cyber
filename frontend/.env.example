# Frontend environment configuration
#
# For local development leave VITE_API_URL unset - the Vite dev server proxies
# /api to the local backend (see vite.config.ts).
#
# For production builds, point it at the deployed Render backend:
#   VITE_API_URL=https://refinery-finance-api.onrender.com
VITE_API_URL=
